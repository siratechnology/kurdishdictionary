<Project Sdk="Microsoft.NET.Sdk.Web">

  <ItemGroup>
    <ProjectReference Include="..\shared\Shared.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Client for the backend's ActivityHub — this app listens, it does not host that hub. -->
    <PackageReference Include="Microsoft.AspNetCore.SignalR.Client" Version="8.*" />

    <!-- Telerik UI for Blazor, from the local commercial feed "last telerik"
         (C:\Telerik Lasted Version). NOT on nuget.org — a machine without that NuGet source
         registered cannot restore this project. -->
    <PackageReference Include="Telerik.UI.for.Blazor" Version="6.0.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <RootNamespace>frontend_blazor</RootNamespace>
    <AssemblyName>$(AssemblyName.Replace(' ', '_'))</AssemblyName>
    <BlazorDisableThrowNavigationException>true</BlazorDisableThrowNavigationException>
  </PropertyGroup>

  <Target Name="NpmInstall" BeforeTargets="BuildTailwind"
          Condition="'$(SkipNpmBuild)' != 'true' AND !Exists('$(MSBuildProjectDirectory)/node_modules')">
    <Exec Command="npm install" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>

  <Target Name="BuildTailwind" BeforeTargets="Build"
          Condition="'$(SkipNpmBuild)' != 'true'">
    <MakeDir Directories="$(MSBuildProjectDirectory)/wwwroot/css" />
    <Exec Command="npm run build:css" WorkingDirectory="$(MSBuildProjectDirectory)" />
  </Target>

</Project>
